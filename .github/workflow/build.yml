name: build

author: MrHuangJser

description: build lib for ghpage

on:
  push:
    branches:
      - master

jobs:
  build-app:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    setps:
      - uses: actions/checkout@v2
      - uses: borales/actions-yarn@v2
        with:
          cmd: install
      - uses: borales/actions-yarn@v2
        with:
          cmd: build:app
